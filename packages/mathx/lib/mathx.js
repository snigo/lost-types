const approx = require('./approx');
const getPrecision = require('./get-precision');
const modulo = require('./modulo');
const random = require('./random');
const round = require('./round');
const toNumber = require('./to-number');

module.exports = {
  approx,
  getPrecision,
  modulo,
  random,
  round,
  toNumber,
};
